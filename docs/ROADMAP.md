# Epoch 实施路线

状态：Draft v0.4
日期：2026-07-21

## 1. 文档职责

本文只负责项目阶段、交付物、依赖关系和验收标准，不承载产品需求、技术设计或投资规则。

- 产品范围与用户体验见 [DESIGN.md](DESIGN.md)；
- 系统结构与工程约束见 [ARCHITECTURE.md](ARCHITECTURE.md)；
- 投资原则与策略规则见 [STRATEGY.md](STRATEGY.md)。

## 2. 实施原则

1. 先得到可信账本，再建设组合与风险智能；
2. 先建立确定性计算和收口，再接入 Agent 判断；
3. 先使用合成、脱敏和历史数据，再接入真实账户；
4. 每个阶段都产生可独立验证的增量，不等待所有模块完成后一次交付；
5. Agent 只产生研究、判断和意向，不替代计算引擎、Policy Gate 或最终决定；
6. 全程保持券商交易权限只读，真实交易在系统外手工执行；
7. 不因未来可能商业化或拆分多 Agent 而增加当前复杂度。
8. 保持单一 Git 仓库和一条本地启动链路；TypeScript 控制面与 Python 计算面以版本化契约隔离。

## 3. 阶段路线

### Phase 0：项目基础与策略骨架

状态：已完成（2026-07-21），验收记录见 [reviews/PHASE_0.md](reviews/PHASE_0.md)。

目标：建立可重复开发环境，将首个卫星仓的口径、策略与参数纳入版本管理。

交付：

- 仓库目录、开发环境和基础 CI；
- Web、API 和 PostgreSQL 骨架；
- 数据库迁移与轻量任务调度机制；
- 时间、时区、交易日、货币和证券标识约定；
- 合成 Demo 数据集；
- `StrategyVersion`、`ParameterSet` 与策略 Schema；
- 个人配置的初始版本。

需要所有者确定：

- 卫星仓账户边界；
- 默认基准 `.NDX`；
- 基础币种、报告币种与风险计算的 USD 口径；
- 首个策略版本与当前待校准参数。

完成标准：

- 任意开发环境可一条命令启动；
- 固定输入可以重现相同基础计算结果；
- 策略和参数可版本化，但 Agent 与页面不能直接修改；
- 仓库不包含真实凭证或真实账户数据。

### Phase 1：可信账本与组合表现

状态：已完成（2026-07-22，合格对账）。现有真实清洗基线已完成契约校验、内容哈希、幂等数据库登记、数据库优先读取、Flex CSV 增量导入入口、仓位数量与现金端点对账、现金流权重与逐日资产收益对账、TWR/MWR 指标、逐日状态重放和逐日估值。无法从富途导出的历史期权与子账户日终市值采用显式残差桥接，并作为限定条件持续展示。

当前对账证据：真实 PostgreSQL 基线包含 536 条账本事件、113 条报告持仓和 545 个连续绩效点；回查原始月结单后，已补入 TSLA 赠股（2025-03-24，+0.3136）、TSLA 赠股（2025-06-24，+0.4004）和 02259 IPO 获配（2025-09-26，+100）三笔显式非现金调整事件，并排除 2025-08-05 博时美元货币基金赎回的重复现金摘要行。另已从 IBKR 活动报表补齐两次 USD/KRW 换汇缺失的 USD 对手腿和各 2 USD 佣金，从富途月结单补齐 8 笔子账户资金调拨、TCOM 卖出看跌期权及行权关闭、POP 卖出与买入平仓三笔期权成交，并按所有者确认补入 2026-02-27 富途 62 HKD 融资利息。全部 536 条事件均通过分类型字段契约；三只货币基金已按现金等价物从证券数量对账中剔除，处理 8 笔跨时区边界交易后，现有相邻报告快照的 71 个有效市场证券数量比较全部一致；零仓位到零仓位的完全抵消区间不计入有效比较。

当前绩效证据：545 个连续日频点的净值链最大链接误差为 `5.01e-9`，全区间 TWR 为 `+49.16%`；使用期初资产、逐日外部资金流和期末资产计算的 XIRR 为年化 `+29.94%`，对应全区间累计 MWR `+47.75%`。四个外部现金流日均已记录显式 Modified Dietz 权重，逐日资产收益最大复算误差为 `3.95e-5`，通过由最早 Futu 四位小数收益精度确定的 `5e-5` 容差。

当前估值证据：113 条报告持仓均已携带显式券商基础币种估值。105 条 Futu 行以 CNY 为基础币种，全部可在人民币 1 分容差内复算，最大误差为 `0.0050 CNY`；8 条 IBKR 行以 USD 为基础币种，其中 KRW 资产使用 Activity Statement 的报告日汇率 `0.00067206 USD/KRW`，USD 资产汇率为 1，全部在源数据精度内复算。报告日估值换汇覆盖已达到 113/113。`HK0000502390`、`HK0000584752`、`HK0000938420` 已按现金等价物归类，基金申赎作为现金管理内部转换，不再构成证券级日频行情硬依赖；已取得的华夏与高腾官方 NAV 继续用于辅助核验。扣除三只现金等价物后，日频行情覆盖审计将 34 个券商证券代码归并为 28 个真实市场标的（折叠 6 个 Futu/IBKR 美股代码别名）；`2025-01-20` 至 `2026-07-18` 已留存并标准化 11,148 条观测，市场标的达到 28/28，`HKDUSD`、`KRWUSD` 汇率达到 2/2，同时识别 6 次拆股事件。

当前重放输入证据：536 条事件已全部映射为 258 笔日频市场交易、3 笔期权交易、94 笔现金等价物申赎、141 笔现金事件、16 条换汇现金腿、20 条账户迁移/调拨和 4 笔非现金调整；85 笔货币基金申赎已区分现金生效日与份额结算日。545 个日历日状态已确定性应用 536/536 条交易和 6/6 次拆股；银行在途、7313 子账户调拨和 IPO 冻结资金分别记账，避免不同在途类别彼此冲销。期末现金端点现已闭合 4/4：IBKR USD/KRW 在源数据精度内一致，富途 USD 差异为 0.03 USD 舍入差，富途 HKD 在补入已确认的 62 HKD 融资利息后完全闭合。普通证券、现金、银行在途资金和三只现金等价物已支持 USD 逐日估值：467 天使用独立行情完整估值；富途无法导出历史子账户或期权逐日市值，因此另外 78 天使用显式残差桥接（19 天 7313 子账户、58 天历史期权、1 天周末券商快照时点），以组合报告总资产减去全部已知资产，推导缺失组件的合并日终市值。系统保留“独立估值”与“残差桥接”标记，不将推导值伪装成市场报价，545/545 天均已入账。月结单证明 7313 是组合内子账户，最后一笔 `9,869.06 USD` 于 2025-03-03 转回后账户关闭；调拨现金差额 `2,182.48 USD` 是该子账户期间损益而非期末在途资产。IBKR 周日隔夜成交在下一交易日生效，周末估值沿用前一交易日证券和汇率，避免把周末 FX 波动错误计入券商已结算资产。

Phase 1 最终验收：所有差异均已定位到事件、估值来源基差或显式残差组件；467 个独立估值日相对券商组合资产的最大来源基差为 `0.532%`，低于 `0.75%` 合格对账阈值；78 个残差桥接日均保留原因分类。基线检查现返回 `ledgerReconciled: true`。

目标：准确回答“组合发生了什么”。

交付：

- IBKR Flex 原始报表导入；
- 幂等同步和不可变原始数据留存；
- 交易、现金、费用、股息和公司行动归一化；
- 每日 PositionSnapshot 与 PortfolioSnapshot；
- NAV、TWR、MWR、回撤和 `.NDX` 基准比较；
- Data Health、账本守恒和对账视图。

完成标准：

- 选定历史区间与 IBKR statement 对账；
- 差异可以定位和解释；
- 重复导入不会生成重复记录；
- 关闭所有模型和 Agent 后仍可完整运行。

### Phase 2：持仓穿透与市场数据基础

目标：准确回答“组合现在持有什么，又暴露于什么”，并为风险计算建立可信输入。

交付：

- IBKR Web API 只读连接；
- 按需盘中持仓、账户摘要和行情状态；
- 日频 OHLC、基准和汇率数据；
- 证券、发行人、ETF 底层持仓与主题映射；
- 行业、地域、币种、主题和发行人穿透；
- 持仓重叠、收益归因和风险来源视图；
- 数据过期、连接状态与最后可信快照。
- Analytics 输入快照与输入/输出 JSON Schema v1，在风险模型实现前固化日期、单位、精度、缺失值和版本约定。

完成标准：

- 可以从组合异常下钻到持仓和暴露来源；
- 可以识别不同证券背后的共同发行人或主题；
- 可以构建对齐交易日的 USD 收益序列；
- IBKR 断线或上游数据失效不会污染最后可信状态。

### Phase 3：确定性风险引擎与 Policy Gate

目标：准确回答“应该承担多少风险，一个调仓意向是否越过红线”。

本阶段分为递进的两个内部里程碑：

#### Phase 3A：风险核心与可重现管线

交付：

- `CalculationRun` 与输入/输出快照；
- 单仓库内的 Python Analytics Service、内部健康检查与版本化计算 API；
- TypeScript Scheduler 到 Analytics Service 的超时、重试、幂等、契约校验和失败记录；
- 历史波动率、协方差、相关性和基线 HAR-RV；
- ERC、RC、组合波动率与观点权重合成；
- 历史 CVaR 与历史崩盘周压力情景；
- 当前策略的 RC、组合波动率、财报日、压力情景与回撤校验；
- 对调仓意向进行组合重算的 Policy Gate；
- 数据缺失、计算失败与降级状态。

#### Phase 3B：HAR-IV-J 核心预测

交付：

- 日频 OHLC 的已实现波动率口径；
- IBKR 期权链快照与 30 天 ATM IV 序列；
- 跳跃项口径及财报 gap、涨跌停与汇率叠加样例；
- HAR-IV-J 训练、预测、回测和模型版本；
- IV 缺失、期权不活跃、新上市标的与不适用品种的降级/拒绝策略；
- 预测波动率与实际已实现波动率的误差记录。
- Python 纯计算包、HTTP 服务外壳与黄金样本重放 CLI 共享同一模型实现。

完成标准：

- 固定数据快照、模型、策略和参数版本可重现相同结果；
- RC、目标权重、组合波动率、CVaR 和压力测试均有黄金样本；
- Policy Gate 可对任意目标持仓输出逐条、可解释的校验结果；
- 数据或模型不可用时显示降级状态，不伪装成正常预测；
- 关闭外部 Agent 后，整个计算与校验管线仍正常运行。
- 单条 Docker Compose 命令可启动 PostgreSQL、Web/API、Scheduler 和 Analytics 四个常驻服务，Analytics 不对主机公开端口。

### Phase 4：Operations、事件视界与 Journal

目标：准确回答“今天有什么必须处理，当时为什么这样做”。

交付：

- Operations 首页与每日运行状态；
- 数据过期、计算失败、风险预警、交易触发和硬约束待办；
- 事件、近区/远区事件视界与每日滚动；
- 缺少预案、催化剂过期和季度重构待办；
- Theme、Candidate、FactorAssessment、ViewAllocation、Evidence 和 Claim；
- Catalyst、InvalidationCondition 与 ExitRestriction；
- Playbook、PlaybookBranch 与 ExceptionRecord；
- RebalanceIntent、PolicyEvaluation、Decision 和 ExecutionRecord；
- 日常、周度、月度、季度和平仓后复盘。

本阶段允许所有者手工写入研究结果和预案，不依赖 Agent 即可验证完整领域闭环。

完成标准：

- 一次入场、复核、调仓和平仓可以完整还原；
- 历史判断与决定绑定当时的策略、参数、数据和计算版本；
- 事件进入近区但缺少预案时产生明确红旗；
- 预案外动作只能通过特事特看对象留痕；
- 系统建议、机械校验、最终决定和实际执行分开保存。

### Phase 5：通用 Epoch Investment Agent

目标：让 Codex 或 Claude 通过一个通用 Agent 使用 Epoch 的数据、计算和投资策略完成研究与判断。

交付：

- `epoch-investment-agent` Skill；
- 统一 Agent Gateway、JSON Schema 和本地 CLI/API；
- `research_candidate`、`review_position`、`review_portfolio`；
- `prepare_event`、`assess_event`、`propose_rebalance`、`run_review`；
- 受限查询、有限写入与 Policy Gate 调用权限；
- AgentRun 审计、策略版本、来源引用与人工反馈；
- 固定证据集与回归评估集。

完成标准：

- 一个 Agent 可根据 `task_type` 完成全部七类任务；
- 研究结果区分事实、假设和推论，并提供来源与置信度；
- 六因子评估与观点权重包含支持、反证和权重差异理由；
- Agent 调用 Epoch 生成客观计算和 PolicyEvaluation，不自行写入结果；
- Agent 不能读取未授权字段，不能修改账本、策略、参数、计算、最终决定或真实交易；
- 外部 Agent 不可用时，账本、绩效、风险计算、Policy Gate、Operations 和 Journal 仍正常运行。

### Phase 6：长期运行与模型增强

候选范围：

- HARQ 与其他经回测验证的预测增强；
- GEX、隐含相关性、融资余额、期权杠杆和拥挤度数据源增强；
- 数据源失效监控与冗余；
- 研究记忆检索与长周期命题追踪；
- 预测误差、置信度、预案覆盖与决策质量统计；
- 将稳定、重复的 Agent 判断下沉为确定性信号或规则；
- 多设备只读访问；
- 订单草稿能力的独立安全评审。

Phase 6 不属于 MVP 承诺。

## 4. MVP 定义

MVP 包含 Phase 0 至 Phase 5。各阶段必须产生可使用增量，但 MVP 结束时必须形成从可信账本到 Agent 判断与人工决定的完整闭环。

MVP 端到端验收：

1. 导入至少一年的 IBKR 历史数据并通过账本对账；
2. 与 `.NDX` 比较收益、回撤和风险；
3. 从绩效异常下钻到证券、发行人、主题和其他暴露来源；
4. 使用版本化的 HAR-IV-J 或明确标记的降级模型生成波动率预测；
5. 计算 ERC、RC、目标权重、组合波动率、CVaR 和压力情景；
6. 对一个调仓意向输出可重现、逐条的 PolicyEvaluation；
7. 事件视界发现近区事件缺少预案并生成待办；
8. 通用 Agent 完成一次候选研究、六因子评估、观点权重与预案编写；
9. Agent 生成调仓意向，Epoch 独立执行 Policy Gate，所有者确认、修改或拒绝；
10. Journal 还原当时的数据、计算、证据、Agent 判断、校验、决定和实际执行；
11. 外部 Agent 不可用时，账本、风险管线、Operations 与 Journal 不受影响；
12. 系统不具备真实下单能力。

## 5. 里程碑依赖

```text
Phase 0  项目基础 / 策略版本
   ↓
Phase 1  可信账本 / 组合表现
   ↓
Phase 2  持仓穿透 / 市场数据
   ↓
Phase 3  风险计算 / Policy Gate
   ↓
Phase 4  Operations / 事件视界 / Journal
   ↓
Phase 5  通用 Agent
```

可并行的工作：

- HAR-IV-J 方法细化、Python 依赖验证和期权数据积累可提前开始，但只在 Phase 3 进入生产计算；
- 策略 Schema、Agent 输出 Schema 和回归评估集可与 Phase 2–4 并行设计；
- 事件日历数据源可与 Phase 3 并行接入；
- Agent 不应早于核心领域对象、确定性计算和 Policy Gate。

## 6. 首个开发切片

第一个切片只完成：

1. 使用合成数据导入一组交易和现金流；
2. 生成每日持仓和 NAV；
3. 计算 `.NDX` 基准的累计收益；
4. 显示组合与基准曲线；
5. 展示账本守恒和数据健康状态。

该切片验证最基础的领域模型和计算口径，不依赖真实 IBKR 凭证、期权数据、事件数据或外部 Agent。
